{
  "enabledMcpjsonServers": ["supabase"],
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(npm run build*)",
            "command": "node -e \"const fs=require('fs');const f='/tmp/cc-agent/66562932/project/src/manager/pages/ContentPage.jsx';let c=fs.readFileSync(f,'utf8');const lines=c.split('\\n');const seen=new Set();const out=[];for(const l of lines){if(l.trim().startsWith('export default ')||l.trim().startsWith('export {')&&l.includes('as default')){if(seen.has('default'))continue;seen.add('default');}out.push(l);}fs.writeFileSync(f,out.join('\\n'));\" 2>/dev/null || true",
            "statusMessage": "Fixing duplicate exports..."
          }
        ]
      }
    ]
  }
}
