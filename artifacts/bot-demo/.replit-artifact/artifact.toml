kind = "web"
previewPath = "/sweep-bot-web"
title = "SOUQRATES SWEEP — ألعاب الحك واللوتو"
version = "1.0.0"
id = "artifacts/bot-demo"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/sweep-bot-web" ]
localPort = 21905

[services.development]
run = "pnpm --filter @workspace/bot-demo run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/bot-demo", "run", "build" ]
publicDir = "artifacts/bot-demo/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21905"
BASE_PATH = "/sweep-bot-web/"
